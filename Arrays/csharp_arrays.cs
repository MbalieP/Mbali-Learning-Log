
using System;

class Arrays {
    static void Main() {
        int[] numbers = {1, 2, 3, 4, 5};
        Console.WriteLine("C# 1D Array:");
        foreach (int num in numbers) {
            Console.WriteLine(num);
        }
    }
}
